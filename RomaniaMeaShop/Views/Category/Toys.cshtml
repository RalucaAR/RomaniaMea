@model IEnumerable<RomaniaMea.Models.Product>

<h1>Jucării</h1>
@if (Model.Count() > 0)
{
    <div class="row">
        @foreach (var product in Model)
        {
            @await Html.PartialAsync("ProductView", product);
        }
    </div>
}
else
{
<p>Ne pare rău dar nu am gpsit nicio jucărie! &#128546; </p>
}
